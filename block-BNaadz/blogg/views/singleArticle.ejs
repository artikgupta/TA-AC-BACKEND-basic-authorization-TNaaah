<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
     <h2><%= article.title %></h2>
     <p><%= article.description %></p>
     <h5>likes: <span><%= article.likes%></span></h5>
     <div class="like">
        <a href="/articles/<%= article.id%>/likes">
          <button>Like</button>
        </a>
      </div>
      <form action="/articles/<%= article.id %>/comments" method="POST">
          <label for="content">Add Comment</label>
            <textarea name="content" id="conent" placeholder="add comment"></textarea>
            <input type="submit" value="Add Comment">
      </form>

      <% if (article.commentId.length) { %>
       <% article.commentId.forEach(comment => { %>
         <p> <%= comment.content %></p>
         <h5> <span>Likes</span>:<%= comment.likes %></h5>
         <a href="/articles/<%= article.id %>/comments/<%= comment.id %>/likes">
          <button>Like</button>
        </a>
         <a href="/articles/<%= article.id %>/comments/<%= comment.id %>/edit">
          <button>Edit</button>
        </a>
        <a href="/articles/<%= article.id %>/comments/<%= comment.id %>/delete">
          <button>Delete</button>
        </a>
      <% });%>
      <% } %>
</body>
</html>